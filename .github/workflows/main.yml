name: coverall

on: [push,pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Coveralls GitHub Action
      uses: coverallsapp/github-action@v1.0.1
      with:
        github-token: OxkOnjeBKbwiISluG5D1iQMbwiGd0IYWr
